# Taking input from user
user_input = str(input("Enter a Sentence: ")
# making list of word
text = user_input.split()
# creating empty string
a=""
# Acessing each word one by one using loop
for i in text:
# Taking frist letter of word and converting to uppercase
 a=a+str(i[0].upper())
# Printing acronym
print(a)
